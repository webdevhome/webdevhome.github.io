<a class="link-item" href="{{ item.url }}">
  <img src="/images/{{ folder }}/{{ item.image }}.png" alt="" class="link-item__image">
  @if(item.title)
    <div class="link-item__label">{{ item.title }}</div>
  @endif
</a>
